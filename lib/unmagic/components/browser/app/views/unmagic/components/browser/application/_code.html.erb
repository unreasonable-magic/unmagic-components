<%# A block of source with a copy button: the gem's own code view, coloured as
    ERB. id: names the wrapper; the <code> the button copies is "#{id}_code". %>
<%= code_view source, language: :erb, id: id, class: "rounded-xl" %>
