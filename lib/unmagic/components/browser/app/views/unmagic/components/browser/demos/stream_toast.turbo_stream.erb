<% if %w[content actions leading custom].include?(params[:example]) %>
  <%= render partial: "unmagic/components/browser/examples/toast/#{params[:example]}",
        formats: [ :turbo_stream ], locals: { options: @toast_options } %>
<% else %>
  <%= turbo_stream.toast(params[:message], **@toast_options) %>
<% end %>
